'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _axios = require('axios');

var _axios2 = _interopRequireDefault(_axios);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = function (symbols) {
    var symbolStr = symbols.map(function (s) {
        return '"' + s + '"';
    }).join(',');
    return _axios2.default.get('http://query.yahooapis.com/v1/public/yql', {
        params: {
            q: 'select * from yahoo.finance.quotes where symbol in (' + symbolStr + ')',
            format: 'json',
            env: 'store://datatables.org/alltableswithkeys'
        }
    }).then(function (response) {
        return response.data.query.results.quote;
    });
};